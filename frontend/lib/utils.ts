/**
 * Shared utility functions.
 */

// Currently empty — utilities added as needed.
// The cn() function (clsx + twMerge) was removed as unused.
// Re-add it if needed: import { clsx } from "clsx"; import { twMerge } from "tailwind-merge";
